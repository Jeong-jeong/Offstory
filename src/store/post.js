import { API_ENDPOINT_DEVCOURSE, API_ENDPOINT_AREA } from '../utils/variables'

export default {
  namespaced: true,
  state() {
    return {
      postList: [],
    }
  },
  getters: {},
  mutations: {},
  actions: {
    async postFetch() {},
  },
}
